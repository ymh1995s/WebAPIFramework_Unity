# UnityMCP를 사용한 구현에 대한 요구사항
- 씬 단위로 작업할 것
- 작업이 이미 되어 있다면 올바르게 되어있는지 검토

## 게입 시작 ~ 로그인씬 진입 사이 
- 서버가 점검 중이면 UI_ServerMaintenancePopup 소환
  - Confirm 버튼 클릭 시 어플리케이션 종료
- 업데이트가 필요할 시 UI_UpdatePopup 소환
  - Confirm 버튼 클릭 시 디버그로그만 찍고 이후 로직 속행
- 자동 로그인 : 기존 로그인 정보가 있다면 자동 메인씬으로 전환 
- 이용 약관 동의 : 즉시 게임 가능하고 첫 실행 시, UI_TermsPopup 소환 ..? 아닌가 어느타이밍이 제일 적절한지 네가 판단
  - 서버자 점검중이 아니고, 업데이트가 필요 없어야 함
  - Confirm 버튼 클릭 시 이후 로직 속행 

## LoginScene
- GuestLogin Btn : 게스트 로그인
- GoogleLogin Btn : 구글 로그인

## MainScene
- 구글 연동 버튼 : 게스트 로그인일 때, 구글과 연동 (이미 구글 연동이면, 유니티 디버그 로그에 실패 출력)
- 메일 박스 버튼 : UI_MailPopup 프리팹 소환. 
  - UI_MailPopup Text에 메일로 받은 아이템 목록을 string으로 출력, Exit 버튼은 팝업 닫기, 전체 수령 버튼은 메일박스의 전체 아이템을 인벤토리로 이동
  - 텍스트에 아이템 목록을 글자로 표현. ex) {일반검, 1개, ~3일}
- 랭킹 버튼 : 자신의 랭킹이 몇위인지 유니티 디버그 로그에 출력
- 인벤토리 : UI_ConfimPopup 소환.
  - UI_ConfimPopup에서 Text에 인벤토리의 아이템 목록을 string으로 출력 (포멧은 UI_SelectPopup에서 참고), Ok 버튼을 누르면 팝업 닫기.
  - 텍스트에 아이템 목록을 글자로 표현. ex) {일반검, 1개, ~3일}
- 공지사항 : UI_ConfimPopup 소환
  - UI_ConfimPopup에서 Text에 공지사항 리스트를 string으로 출력, Ok 버튼을 누르면 팝업 닫기.
- 문의 : UI_InquiryPopup 소환
  - UI_InquiryPopup에서 문의하기 버튼 클릭 시 서버로 문의 사항 전달, 메세지는 고정 {누구누구의 문의입니다} 포멧으로 전송
  - 텍스트에 문의 목록과 결과를 표현, 각 문의마다 개행할 것
- 광고 버튼 : 구현하지 않는다.
- 인앱결제 버튼 : 구현하지 않는다.
- 스테이지 선택 버튼 : 스테이지셀렉트 씬으로 전환된다
- 로그아웃 버튼 : 로그아웃하고 로그인씬으로 전환
- 계정 삭제 : UI_SelectPopup 소환
  - Ok 버튼 클릭시 계정 삭제후 로그인씬으로 전환된다
  - 캔슬 버튼 클릭 시 팝업 닫음

## StageSelectScene
- 각 스테이지 버튼 : 게임씬으로 전환된다. => 실제 서비스는 더 복잡할테니 적절한 프레임워크를 설계할 것
- 메인 메뉴 버튼 : 메인씬으로 전환된다.

## GameScene
- 텍스트 : 현재 몇 스테인지 표기
- 성공 버튼 : 현재 스테이지를 클리어했다고 서버에 요청한 후, UI_RewardPopup 생성한다
  - UI_RewardPopup의 Text에 보상 목록을 string으로 출력한다 (서버에서 결과를 받아옴)
  - UI_RewardPopup의 OK 버튼 : 메인씬으로 전환된다.
- 실패 버튼 : 메인씬으로 전환된다.

## 기타
- 각종 에러 시 UI_ErrorPopup 소환
  - Text에 에러 메시지 표기
  - OK 버튼 클릭 시 게임 재시작
- 각종 안내 필요 시 UI_AnnouncementPopup 소환
  - Text에 안내 메시지 표기
  - OK 버튼 클릭 시 팝업 닫기