# MANUAL_TEST — 수동 테스트 시나리오

**기준 문서**: [CLIENT_GUIDE.md](../CLIENT_GUIDE.md)  
각 항목 끝의 `(→ §N)`는 CLIENT_GUIDE.md 해당 섹션을 가리킨다.  
신규 기능이 CLIENT_GUIDE.md에 추가될 때 → 이 파일에 해당 시나리오를 추가한다.

**프레임워크 전용 항목**은 `[FW]` 접두사로 표시 (CLIENT_GUIDE.md 섹션 없음).

### 새 시나리오 추가 원칙

이 파일은 **사용자 여정 순서**로 구성되어 있다. 항목을 추가할 때는 아래 기준으로 위치를 결정한다.

| 추가할 기능 유형 | 삽입 위치 |
|---|---|
| 앱 실행 초기 흐름 (부팅, 공지 등) | 시나리오 1 내부 |
| 로그인/계정 생성 흐름 | 시나리오 1~4 내부 |
| 로그인 후 콘텐츠 기능 | 시나리오 5 내부 (5-1, 5-2… 번호 이어서) |
| 오류·엣지 케이스·계정 관리 | 시나리오 6 내부 (6-1, 6-2… 번호 이어서) |
| 시각/UX 프레임워크 | 시나리오 7 내부 |

> **순서 원칙**: 사용자가 앱을 처음 켰을 때부터 반복 사용까지의 흐름을 유지한다.  
> 계정이 없는 상태에서 시작하여 → 가입 → 콘텐츠 탐색 → 예외 상황 순으로 이어진다.  
> 독립적인 기능이라도 "이 기능을 처음 만나는 순간은 언제인가?"를 기준으로 위치를 결정한다.

---

## 테스트 환경

| 항목 | 값 |
|---|---|
| Unity | 2022.3.x LTS |
| API Base URL | `http://localhost:5000` (로컬) |
| 서버 실행 | `dotnet run --project Framework.Api` |
| Admin | `http://localhost:5001` (Framework.Admin) |
| 테스트 디바이스 | Unity 에디터 / Android 에뮬레이터 |

---

# 시나리오 1 — 첫 실행 (계정 없음, 게스트 가입)

앱을 최초로 설치한 사용자가 게스트 계정을 생성해 메인 로비에 진입하는 전체 흐름.

## 1-1. 점검 모드 통과 (→ §1)

**선행 조건**: 서버 실행 중. Admin에서 점검 모드 OFF.

| # | 단계 | 확인 기준 |
|---|---|---|
| 1 | 앱 실행 | 점검 팝업 미표시, 다음 단계로 진행 |

> 점검 모드가 ON이면 **시나리오 6-1**로 이동.

---

## 1-2. 강제 업데이트 통과 (→ §2)

**선행 조건**: Admin에서 현재 클라이언트 버전에 `isForceUpdate=false` 설정.

| # | 단계 | 확인 기준 |
|---|---|---|
| 1 | 부팅 진행 중 | 강제 업데이트 팝업 미표시 |

> 강제 업데이트 상황이면 **시나리오 6-2**로 이동.

---

## 1-3. 최신 공지 확인 (→ §3)

**선행 조건**: Admin에서 활성 공지 등록.

| # | 단계 | 확인 기준 |
|---|---|---|
| 1 | 부팅 완료 후 | 공지 팝업 표시 |
| 2 | "닫기" 클릭 | 팝업 닫힘, 로그인 화면 표시 |

> 공지가 없으면 팝업 없이 로그인 화면 바로 표시.

---

## 1-4. 게스트 로그인 (→ §4)

| # | 단계 | 확인 기준 |
|---|---|---|
| 1 | 로그인 화면에서 "게스트로 시작" 클릭 | 로딩 후 메인 씬 진입 |
| 2 | 메인 진입 직후 | "일일 보상이 우편함에 도착했습니다" 토스트 표시 (→ §11) |

---

## 1-5. 메인 로비 기본 탐색

| # | 단계 | 확인 기준 |
|---|---|---|
| 1 | 메인 씬 HUD 영역 확인 | Admin에서 등록한 외침(Shout)이 표시됨 (→ §12) |
| 2 | 우편함 버튼 클릭 | 일일 보상 우편 수령 가능 (→ §13) |
| 3 | 보상 우편 "수령" 클릭 | 수령 완료, 아이템/Exp 반영 (→ §14) |

---

# 시나리오 2 — 기존 게스트 계정 재접속

게스트 로그인 후 앱을 종료하고 재실행하는 흐름.

## 2-1. 자동 게스트 복구 (→ §4)

| # | 단계 | 확인 기준 |
|---|---|---|
| 1 | 앱 종료 후 재실행 | 동일 DeviceId로 동일 게스트 계정 자동 로그인 |
| 2 | 메인 로비 재진입 | 일일 보상 이미 수령 시 토스트 미표시 (멱등) |

---

## 2-2. 포그라운드 복귀 시 세션 갱신 [FW]

| # | 단계 | 확인 기준 |
|---|---|---|
| 1 | 메인 로비에서 앱 백그라운드 전환 | 아무 변화 없음 |
| 2 | 포그라운드 복귀 | 토큰 갱신 + 점검/강제업데이트 재확인 (이상 없으면 UI 변화 없음) |
| 3 | 복귀 직후 복귀 재트리거 | 단일 재개만 수행됨 (_resuming 가드) |
| 4 | RefreshToken 만료 상태에서 복귀 | 로그인 화면으로 이동 (→ §8) |

---

# 시나리오 3 — 게스트 계정에 구글 계정 연동

게스트 상태에서 구글 계정을 연동해 계정을 영구화하는 흐름.

## 3-1. 정상 연동 (→ §6)

**선행 조건**: 게스트 로그인 상태. Google Sign-In SDK 통합 완료.

| # | 단계 | 확인 기준 |
|---|---|---|
| 1 | 설정 화면 "구글 계정 연동" 클릭 | 구글 로그인 UI 표시 |
| 2 | 연동 미사용 구글 계정 선택 | "연동 완료" 토스트 표시 |
| 3 | 연동 후 로그아웃 → 구글 로그인 | 동일 계정 진입, 게스트 데이터 보존 확인 |

---

## 3-2. 충돌 발생 시 (→ §6, §7)

**선행 조건**: 다른 계정이 이미 동일 구글 계정을 보유.

| # | 단계 | 확인 기준 |
|---|---|---|
| 1 | 충돌 구글 계정으로 연동 시도 | 409 충돌 다이얼로그 표시 (기존/현재 계정 정보 표시) |
| 2 | "기존 계정으로 전환" 선택 | 게스트 진행도 영구 손실 경고 팝업 표시 |
| 3 | 경고 확인 | 기존 계정으로 메인 진입, 게스트 데이터 사라짐 |
| 4 | "취소" 선택 | 게스트 상태 유지 |

---

# 시나리오 4 — 구글 계정으로 신규 로그인

다른 기기에서 구글 계정으로 처음 로그인하는 흐름.

## 4-1. 정상 구글 로그인 (→ §5)

| # | 단계 | 확인 기준 |
|---|---|---|
| 1 | 로그인 화면 "구글로 시작" 클릭 | 구글 로그인 UI 표시 |
| 2 | 계정 선택 완료 | 메인 씬 진입 |

---

## 4-2. 구글 로그인 충돌 (→ §5, §7)

**선행 조건**: 다른 게스트 계정이 이미 해당 구글 계정을 보유.

| # | 단계 | 확인 기준 |
|---|---|---|
| 1 | 충돌 구글 계정으로 로그인 시도 | 409 충돌 다이얼로그 표시 |
| 2 | 시나리오 3-2와 동일하게 진행 | — |

---

# 시나리오 5 — 기능 탐색 (로그인 완료 상태)

## 5-1. 우편함 (→ §13/14)

| # | 단계 | 확인 기준 |
|---|---|---|
| 1 | 우편함 진입 | 목록 표시 (제목/본문/아이템/Exp) |
| 2 | mailItems 다중 아이템 우편 | Gold/Gems 등 개별 시각화 확인 |
| 3 | 미수령 우편 "수령" 클릭 | 수령 완료, 목록 갱신 |
| 4 | 이미 수령한 우편 | 비활성(회색) 표시, 수령 버튼 없음 |

---

## 5-2. 인벤토리 / 아이템 사용 (→ 부록 B-2)

**선행 조건**: Consumable 아이템(예: 체력포션) 1개 이상 보유. 수령 후 우편함 → 인벤토리 갱신 흐름과 자연스럽게 이어진다.

| # | 단계 | 확인 기준 |
|---|---|---|
| 1 | 메인에서 인벤토리 버튼 클릭 | 인벤토리 팝업 표시, 보유 목록 텍스트 출력 |
| 2 | InputField 미입력 후 "사용" 클릭 | 토스트 "유효한 아이템 ID를 입력해주세요." |
| 3 | 보유하지 않은 itemId 입력 후 "사용" | 토스트 "보유하지 않은 아이템입니다." |
| 4 | 재화(currency) itemId 입력 후 "사용" | 토스트 "재화는 사용할 수 없습니다." |
| 5 | Consumable itemId 정상 입력 후 "사용" | 토스트 "아이템을 사용했습니다.", 목록 자동 갱신, 수량 1 감소 |
| 6 | 잔량 0 상태에서 동일 itemId 사용 시도 | 400 에러 팝업 (서버 응답 메시지) |
| 7 | 사용 도중 인벤토리 갱신 실패 | 에러 팝업 + "인벤토리 불러오기 실패" 텍스트 |

**비검증 항목**: 409 멱등 충돌은 동일 clientRequestId 강제 재전송 환경에서만 재현 가능 — 일반 수동 테스트에선 비노출 동작이 정상.

---

## 5-3. 스테이지 선택 (→ §15/16)

| # | 단계 | 확인 기준 |
|---|---|---|
| 1 | 스테이지 선택 화면 진입 | sortOrder 순 표시 |
| 2 | 선행 미클리어 스테이지 | 잠금 아이콘 표시 |
| 3 | 클리어 스테이지 | 별 수/최고점수/클리어 시간 표시 |
| 4 | isActive=false 스테이지 | 목록 숨김 |

---

## 5-4. 스테이지 클리어 (→ §17)

| # | 단계 | 확인 기준 |
|---|---|---|
| 1 | 스테이지 최초 클리어 | 결과 화면, 최초 클리어 보상 메시지, 우편함 알림 |
| 2 | 동일 스테이지 재클리어 | 리플레이 보상 메시지 (없으면 없음으로 표시) |
| 3 | 잠긴 스테이지 클리어 시도 | 잠금 안내 표시 |

---

## 5-5. 랭킹 (→ §18)

| # | 단계 | 확인 기준 |
|---|---|---|
| 1 | 랭킹 화면 진입 | 내 순위/닉네임/베스트점수 표시 |
| 2 | 스코어 없는 신규 플레이어 | 숨김 또는 "-" 표시 (정책에 따름) |

---

## 5-6. 문의 제출/조회 (→ §22)

| # | 단계 | 확인 기준 |
|---|---|---|
| 1 | 문의 화면 진입 | 기존 문의 목록 표시 |
| 2 | 내용 작성 후 "제출" | 201 성공, 목록 갱신 |
| 3 | 빈 내용 제출 시도 | 차단 (클라 입력 검증) |
| 4 | 2001자 이상 입력 | 차단 |
| 5 | Admin에서 답변 등록 후 조회 | 답변 내용 및 답변 일시 표시 |

---

## 5-7. HUD 외침 폴링 / 회전 / 만료 (→ §12)

**선행 조건**: 메인 씬 진입 상태. Admin에서 활성 외침(Shout) 등록 가능.

| # | 단계 | 확인 기준 |
|---|---|---|
| 1 | Admin에서 외침 1건 등록 후 메인 진입 | HUD 영역에 메시지 표시 |
| 2 | Admin에서 추가로 외침 1건 더 등록 후 5분 대기 | 폴링이 갱신되어 2개 메시지 4초 간격 회전 시작 |
| 3 | 회전 중 메시지 표시 | 하나가 4초 노출 후 다음으로 전환, 빠짐없이 순환 |
| 4 | Admin에서 모든 외침 비활성화 후 5분 대기 | HUD 자동 숨김 |
| 5 | 외침 `expiresAt`가 회전 도중 만료되도록 등록 | 만료된 메시지는 회전에서 자동 스킵, 모두 만료 시 HUD 숨김 |
| 6 | 메인 → 다른 씬 이동 → 메인 재진입 | 폴링 자동 중단·재시작, 회전 정상 동작 |
| 7 | 백그라운드 5분 이상 → 포그라운드 복귀 | App Lifecycle BootstrapFlow 재실행 후 폴링 재개 |

**비검증 항목**: 동일 메시지 id 세트 감지(SetMessages 생략)는 디버그 로그로만 확인 가능 — 사용자 가시 동작 없음.

---

# 시나리오 6 — 계정 관리 및 오류 상황

## 6-1. 점검 모드 (→ §1)

**선행 조건**: Admin에서 점검 모드 ON.

| # | 단계 | 확인 기준 |
|---|---|---|
| 1 | 앱 실행 | 점검 팝업 표시, "닫기" 버튼 없음 |
| 2 | 30초 대기 | 자동 재시도 (팝업 유지) |
| 3 | 서버 점검 모드 OFF | 팝업 자동 닫힘, 다음 단계로 진행 |
| 4 | 로그인 완료 상태에서 점검 ON | 임의 API 호출 시 점검 팝업 전환 |

---

## 6-2. 강제 업데이트 (→ §2)

**선행 조건**: Admin에서 현재 버전 `isForceUpdate=true` 설정.

| # | 단계 | 확인 기준 |
|---|---|---|
| 1 | 앱 실행 | 강제 업데이트 팝업 표시 |
| 2 | "스토어 이동" 클릭 | 스토어 URL 열림 (플레이스홀더이면 무반응 — 배포 전 GameConfig.cs 설정 필요) |
| 3 | 팝업 외부 터치 | 게임 진입 차단 |

---

## 6-3. AccessToken 자동 회전 (→ §8)

**선행 조건**: 서버에서 AccessToken 만료 시간 단축 (테스트용).

| # | 단계 | 확인 기준 |
|---|---|---|
| 1 | AccessToken 만료 후 임의 API 호출 | 자동 갱신 후 원 요청 재시도 (사용자 개입 없음) |
| 2 | RefreshToken도 만료 시 | 로그인 화면으로 강제 이동 |
| 3 | 동시 다발 401 발생 | refresh가 1회만 수행됨 (로그 확인) |

---

## 6-4. Rate Limit 429 (→ §25)

| # | 단계 | 확인 기준 |
|---|---|---|
| 1 | 짧은 시간 내 API 반복 호출 | "요청이 많습니다" 안내, Retry-After 대기 후 자동 재시도 |
| 2 | 3회 초과 | 사용자에게 재시도 실패 안내 |

---

## 6-5. 밴 계정 (→ §4)

**선행 조건**: Admin에서 대상 계정 밴 처리.

| # | 단계 | 확인 기준 |
|---|---|---|
| 1 | 밴 계정으로 로그인 시도 | 밴 안내 팝업 + 앱 종료 |

---

## 6-6. 로그아웃 (→ §9)

| # | 단계 | 확인 기준 |
|---|---|---|
| 1 | 설정 화면 "로그아웃" 클릭 | 로그인 화면 이동 |
| 2 | 재실행 후 게스트 로그인 | 동일 DeviceId로 동일 계정 복구 |

---

## 6-7. 탈퇴 (→ §10)

| # | 단계 | 확인 기준 |
|---|---|---|
| 1 | 설정 화면 "탈퇴" 클릭 | 법적 의무 안내 팝업 표시 |
| 2 | 팝업 내용 확인 | 캐릭터/아이템/진행도, 미수령 우편, 결제 보상, 재가입 복구 불가 — 4개 항목 모두 포함 |
| 3 | 확인 클릭 | 탈퇴 처리 + 로그인 화면 이동 |
| 4 | 재실행 후 동일 DeviceId로 게스트 로그인 | 신규 계정 생성 (탈퇴 계정 복구 안 됨) |

---

# 시나리오 7 — 프레임워크 시각 검증 [FW]

## 7-1. Safe Area — 노치/홈 인디케이터 대응

| # | 단계 | 확인 기준 |
|---|---|---|
| 1 | iPhone X 이상 또는 노치 기기에서 실행 | UI 콘텐츠가 노치/홈 인디케이터와 겹치지 않음 |
| 2 | 가로/세로 전환 | SafeArea 재계산 후 UI 자동 조정 |
| 3 | Unity 에디터 Device Simulator — iPhone 12 선택 | 세이프 에리어 반영 시각 확인 |

**적용 씬**: LoginScene, MainScene, GameScene, StageSelectScene  
**적용 프리팹**: UI_ConfirmPopup, NoBtnPopup, SelectPopup, MailPopup, InquiryPopup

---

## 7-2. Toast UI — 1회성 알림

| # | 단계 | 확인 기준 |
|---|---|---|
| 1 | 일일 보상 토스트 | 화면에 메시지 2초 노출 후 페이드아웃 |
| 2 | 토스트 노출 중 새 토스트 트리거 | 기존 메시지 즉시 교체, 타이머 재시작 |
| 3 | 다른 팝업과 동시 표시 | 토스트가 팝업 위에 렌더링 (sortingOrder=999) |

---

# 미구현 항목 (추후 추가)

신규 기능이 CLIENT_GUIDE.md에 추가될 때 아래 표에 행을 추가한 뒤, **위의 추가 원칙에 따라 적절한 시나리오 번호 하단에 순서대로** 시나리오를 작성한다.

| CLIENT_GUIDE.md 섹션 | 클라이언트 상태 | 삽입 위치 | 삽입 순서 기준 |
|---|---|---|---|
| §19 IAP 영수증 검증 | 미구현 | 시나리오 5 하단 | 우편함 확인 후 결제로 이어지는 흐름 |
| §20 게스트 결제 차단 | 미구현 | 시나리오 6 하단 | 오류 상황 — 게스트로 결제 시도 |
| §21 광고 SDK | 미구현 | 시나리오 5 하단 | IAP 다음 (둘 다 보상 획득 수단) |

---

# 변경 이력

| 일자 | 변경 |
|---|---|
| 2026-05-09 | 최초 작성 — 계정 없음→가입→재접속→기능탐색→오류 순차 흐름으로 구성 |
| 2026-05-10 | 시나리오 5-2 인벤토리/아이템 사용 추가 (부록 B-2 연동 완료) |
| 2026-05-10 | 시나리오 5-7 HUD 외침 폴링/회전/만료 추가 (§12 연동 완료) |
